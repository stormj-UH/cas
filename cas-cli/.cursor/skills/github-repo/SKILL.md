---
name: github-repo
description: |
  GitHub repository config for cas. Use when creating PRs, managing issues, checking workflows, or performing any gh CLI operations.
  CRITICAL: Always pass --repo pippenz/cas to every gh command.
  Triggers: create PR, merge PR, check PR, GitHub action, workflow, issue, branch, push, pull request
metadata:
  category: version-control
  server: user-github
  managed_by: cas
---

# GitHub — cas

Refer to `~/.cursor/skills/mcp-github/SKILL.md` for tool usage. This skill provides project-specific config only.

## Repository

<!-- keep github-repo -->
<!-- cas:full_name=pippenz/cas -->
| | Value |
|--|--|
| **Owner** | `pippenz` |
| **Repo** | `cas` |
| **Full name** | `pippenz/cas` |
<!-- /keep github-repo -->

## CRITICAL: Always pass --repo

Every `gh` command MUST include `--repo pippenz/cas`:

```bash
gh pr create --repo pippenz/cas --base main --title "..."
gh pr list   --repo pippenz/cas
gh pr merge  --repo pippenz/cas <number> --merge
gh issue list --repo pippenz/cas
```

---

Generated by `cas integrate github`. Run `cas integrate github refresh` if the repo is renamed or transferred.
